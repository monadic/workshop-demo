
Here is the platform you picked from the Workshop Catalog.

    grep prints the parts named in platform-first-try.yaml. The three platform 
    parts are Catalog bundles pinned by digest. The last part is your own app 
    file.

$ grep -E "^  name:|- name:|authored:" platform-first-try.yaml
  name: my-shop-platform
    - name: cert-manager
    - name: traefik
    - name: metrics-server
    - name: shop-web
      authored: "shop-web.yaml"

    Look for cert-manager, traefik and metrics-server, then shop-web.
